# Phase 1 银行交易 Agent 演示脚本

> 本脚本用于面试演示时标准复现。每次演示按顺序执行，确保验收场景全部覆盖。

---

## 准备工作

1. 确保 app-bank 在 **8081 端口**运行
2. 确保 前端在 **5173 端口**运行
3. 打开浏览器访问 `http://localhost:5173` → 目录页点「银行助手」进入

---

## 验收场景（5 轮对话）

### 场景 1：查询账户（标准流程）

**用户**：查一下张三的余额
**小银**：（模型调用 `queryAccount("张三")`）张三（62220001）余额：12850.50 元。最近交易：
- 2026-09-01 工资入账 +18000.00
- 2026-09-05 房贷扣款 -6500.00
- 2026-09-12 信用卡还款 -3200.00

**验收点**：模型正确调用查询工具，余额和最近交易全部正确。

---

### 场景 2：转账成功（标准 HITL 流程）

**用户**：给李四转 200 元，附言 step1-test
**小银**：（模型调用 `transfer("62220002", 200, "step1-test")`）转账确认单已创建（确认码 abc123）：
- **付款方**：62220001（张三）
- **收款方**：62220002（李四）
- **金额**：200.00 元
- **附言**：step1-test
- **付款方剩余余额**：12650.50 元
请告知用户在页面的确认卡片上操作即可，不要重复创建确认单。

**小银**：（等用户回复确认）

**用户**：确认

**（前端）**：出现「💸 待确认转账」卡片，显示收款人、金额、确认/取消按钮

**用户**：点击「确认转账」

**小银**：（用户确认后，模型调用 `confirmOrder("abc123", true)`）转账成功！
- **付款账户**：62220001（张三）
- **收款账户**：62220002（李四）
- **转账金额**：200.00 元
- **附言**：step1-test
- **剩余余额**：12650.50 元

**验收点**：
- 模型只创建确认单，不直接执行
- 卡片从出现到确认，流程完整
- 资金变动正确（张三余额 12850.50 → 12650.50）

---

### 场景 3：取消转账

**用户**：取消刚才的转账

**小银**：（调用 `queryTransferOrder("abc123")`）确认单 abc123：62220001 → 62220002，金额 200.00 元，当前状态：已取消。需要时请重新发起转账。

**验收点**：模型能查到确认单的真实状态（来自数据库），而不是凭记忆说“还在等待确认”。

---

### 场景 4：重复提交幂等（用户连点两次）

**用户**：再给李四转 300 元，附言 idempotent-test

**小银**：（创建新确认单，confirmId=xyz789）

**用户**：（快速点击两次「确认转账」按钮）

**小银**：
- 第一次点击：✅ 转账成功（余额 12650.50 → 12350.50）
- 第二次点击：「该确认单已执行过，本次重复操作被幂等拦截，资金未变动」

**验收点**：同一张卡片连点两次，只有第一次生效，第二次被状态机拦截。

---

### 场景 5：黑名单预检（直接拒绝，无卡片）

**用户**：给赵六转 100 元，附言 blacklist-test

**小银**：（调用 `transfer("62220004", 100, "blacklist-test")` → 规则预检拦截）转账被风控拦截——收款账户在风控名单中，禁止转账。该收款账户目前无法接收转账，请勿尝试向该账户转账。如需其他帮助，请随时告诉我。

**验收点**：
- 黑名单在创建确认单前就被拦截
- **没有弹出确认卡片**
- 模型如实转述拒绝原因，没有变体重试

---

### 场景 6：日累计超限（第二次转账被拦）

**用户**：给李四转 9900 元（已知今天已转 5000）

**小银**：（预检检测到日累计 5000 + 9900 = 14900 > 10000）转账被风控拦截——当日累计转账不能超过 10,000.00 元，今日已转 5,000.00 元。您今日还可转账 **4,500.00 元**。如需继续转账，建议明日再试或调整金额。

**验收点**：规则正确拦截，且模型给出合理的剩余额度提示（来自 `transferredToday` 查询）。

---

## 附加场景（可选，用于更全面的验收）

### 场景 7：查询不存在账户

**用户**：查一下 99999999 这个账户
**小银**：未找到账户「99999999」。当前可用账户：62220001（张三）余额 12650.50 元；62220002（李四）余额 3620.00 元；62220003（王五）余额 98760.00 元

**验收点**：模型先调用 `listAccounts`，再从结果里告诉用户“查不到”，而不是直接瞎编。

### 场景 8：删除旧确认单（如果需要重置演示）

在数据库里执行：
```sql
DELETE FROM bank_transfer_order;
DELETE FROM bank_audit_log;
DELETE FROM bank_transaction WHERE id > 10;
UPDATE bank_account SET balance=12850.50 WHERE account_no='62220001';
UPDATE bank_account SET balance=3420.00 WHERE account_no='62220002';
```

---

## 演示技巧

1. **控制节奏**：用户说确认后，**等 1 秒再点**，让前端有渲染时间
2. **连点幂等**：第二次连点时，可以在同一行里打两个字符（模拟误触）
3. **回答问题**：用户问“那我的钱还在吗？”“会不会扣两次？”时，模型会如实回答（确认单已执行后没有真实资金变动，只是记录）
4. **场景切换**：演示完 5 个场景后，如果演示者主动问“能再演示一次吗？”，再重跑一次

---

## 关键设计点（面试时可以强调）

- **HITL 的必要性**：Step 2 里模型会擅自改金额（6000→5000）和日累计（9900→4900），Step 3 通过“确认卡片 + 状态机”彻底关死了这个漏洞
- **幂等的核心**：`UPDATE ... WHERE status='PENDING'`，并发下只有一支请求能把状态从 PENDING 推进到 EXECUTED，其他请求返回“已处理过”
- **数据库是唯一事实来源**：模型看不见确认单的真实状态（过期/取消），必须靠 `queryTransferOrder` 工具去查数据库，这比“提示词求模型不要瞎说”高级得多
- **Read 宽 Write 窄**：AI 可以查询（只读工具），但写操作（转账）必须经过人类确认，所有写操作最终都落到 `confirmOrder` 的事务里，由状态机控制
